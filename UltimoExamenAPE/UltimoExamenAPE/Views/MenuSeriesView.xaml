<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="UltimoExamenAPE.Views.MenuSeriesView"
             xmlns:local="clr-namespace:UltimoExamenAPE"
             BindingContext="{Binding Source={x:Static local:App.ServiceLocator}
                , Path=MenuSeriesViewModel}">
    <ContentPage.Content>
        <Grid>
            <StackLayout>
                <StackLayout
                VerticalOptions="CenterAndExpand" HorizontalOptions="FillAndExpand"
                Margin="30">
                    <Button Text="Nuevo Personaje" Command="{Binding NuevoPersonaje}"/>
                    <!--<Button Text="Modificar Personaje" Command="{Binding ModificarPersonaje}"/>-->
                    <Button Text="Series" Command="{Binding MostrarSeries}"/>
                </StackLayout>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>